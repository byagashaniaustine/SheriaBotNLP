# Runtime dependencies for the Sheria-Bot WhatsApp API.
#
# Runtime stack (all CPU-only):
#   fine-tuned DistilBERT (intent)  — transformers + torch
#   sentence-transformers (RAG)     — 118 MB multilingual MiniLM
#   fine-tuned flan-t5 (generator)  — transformers + torch  (loaded lazily)
#   answer_bank.json (fallback)     — plain JSON, no deps
#   langid (lang detection)         — pure python

# Pull torch CPU-only wheels so the container stays small (~500 MB vs 2+ GB with CUDA).
--extra-index-url https://download.pytorch.org/whl/cpu

fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.0
python-dotenv>=1.0
httpx>=0.27
transformers>=4.46,<5.0
torch>=2.0
langid>=1.1

# RAG retrieval — multilingual sentence embeddings for semantic search
# over the law knowledge base. Adds ~250 MB to the container.
sentence-transformers>=2.7
numpy>=1.24

# Fast tokenizer support for the flan-t5 generator (loads tokenizer.json
# instead of the SentencePiece-based T5Tokenizer, which would need protobuf).
tokenizers>=0.15
protobuf>=3.20             # belt-and-suspenders — some HF paths still call into it
