# Use PyPI as primary; pull torch CPU-only wheels from PyTorch's index so the
# Railway container doesn't balloon to 2+ GB pulling in CUDA runtime libraries.
--extra-index-url https://download.pytorch.org/whl/cpu

fastapi>=0.110
uvicorn[standard]>=0.29
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
nltk>=3.8
gensim>=4.3
transformers>=4.46,<5.0
torch>=2.0
matplotlib>=3.7
seaborn>=0.13
wordcloud>=1.9
joblib>=1.3
pydantic>=2.0
httpx>=0.27
